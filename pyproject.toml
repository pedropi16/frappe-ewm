[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "frappe_wms"
authors = [{name = "Pedro Pino Otero"}]
description = "Warehouse Management and Execution System for Frappe Framework v16"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = []

[tool.ruff]
line-length = 110
target-version = "py314"
